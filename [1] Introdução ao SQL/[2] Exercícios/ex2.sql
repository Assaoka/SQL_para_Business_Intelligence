-- Ex2: Faça aparecer somente uma consulta de cada vez rodando  somente uma parte da consulta.
-- Selecione com o cursor do primeiro SELECT até o HumanResources.Employee e execute.
SELECT
	*
FROM
	HumanResources.Employee

-- Selecione apenas do segundo SELECT até o Production.Product e execute.
SELECT
	*
FROM
	Production.Product
